import { SpellCardStyleId } from "@/components/print-cards/types";

export const DEFAULT_SPELL_CARD_STYLE_ID: SpellCardStyleId = "minimal";

export const SPELL_CARD_PREVIEW_ORIENTATION = "landscape" as const;
